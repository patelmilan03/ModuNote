rules_version = '2';

// ModuNote Firestore Security Rules
// Deploy in Firebase Console → Firestore Database → Rules tab.
// Data is scoped to the anonymous UID — no other user can read or write it.

service cloud.firestore {
  match /databases/{database}/documents {

    // Each user's notes are private to their anonymous UID.
    match /users/{userId}/notes/{noteId} {
      allow read, write: if request.auth != null
                         && request.auth.uid == userId;
    }

    // Deny all other paths by default.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
